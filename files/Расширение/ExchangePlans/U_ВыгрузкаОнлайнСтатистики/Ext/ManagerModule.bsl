
Функция ПолучитьУзел() Экспорт 
	УстановитьПривилегированныйРежим(Истина);
	
	УзелВозврат = НайтиПоКоду("1");
	Если Не ЗначениеЗаполнено(УзелВозврат) Тогда
		УзелОбъект = СоздатьУзел();
		УзелОбъект.Код 						= "1";
		УзелОбъект.Наименование 			= "Основной";
		УзелОбъект.ОбменДанными.Загрузка 	= Истина;
		УзелОбъект.Записать();
		
		УзелВозврат = УзелОбъект.Ссылка;
	КонецЕсли;
	
	УстановитьПривилегированныйРежим(Ложь);
	
	Возврат УзелВозврат;
КонецФункции